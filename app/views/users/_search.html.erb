<%= form_with url: yield(:path), method: :get, local: true do |f| %>
  <%= f.label :search, "#{yield(:search_name)}検索" %>
  <%= f.search_field :search, class: "form-control" %>
  <%= f.submit "Search", class: "btn btn-primary" %>
<% end %>
